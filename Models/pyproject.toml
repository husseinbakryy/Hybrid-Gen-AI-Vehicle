[project]
name = "hybrid-genai-models"
version = "0.1.0"
description = "Training and inference pipeline for Hybrid-Gen-AI-Vehicle models"
requires-python = ">=3.10"
dependencies = [
    "joblib>=1.4.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "scikit-learn>=1.3.0",
]
